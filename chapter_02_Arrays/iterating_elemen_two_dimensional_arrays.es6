/**
 * Write by wdiasvargas on 03/05/17.
 *   www.williamdiasvargas.com.br
 *
 **/
'use strict'
let averangeTemp = [];

function printMatrix (myMatrix) {
  for(var i = 0; i < myMatrix.length; i++){
    for(var j = 0;j < myMatrix.length; i++){
      console.log((myMatrix[i][j]));
    }
  }
}
printMatrix(averangeTemp)